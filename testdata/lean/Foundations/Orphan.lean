-- Orphan theorem: exists in the corpus but no anchor references it.

namespace QBP

/-- This theorem has no corresponding PROOF-* anchor. -/
theorem unreferenced_orphan (x : ℝ) : x * 1 = x := by
  ring

end QBP
